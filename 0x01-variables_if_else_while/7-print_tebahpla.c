#include <stdio.h>
#include <stdlib.h>
/**
 * main - print lower alphabet in reverse order
 * Return: Always 0
 */
int main(void)
{
	char ka;

	for (ka = 'z'; ka <= 'a'; ka++)
	{
		putchar (ka);
	}
	putchar ('\n');
		return (0);
}
